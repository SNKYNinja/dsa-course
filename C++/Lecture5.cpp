#include <iostream>
using namespace std;

int main() {
    // Bitwise Operators
    
    // int a = 4, b = 6;

    // cout << " a&b " << (a&b) << endl; // AND
    // cout << " a|b " << (a | b) << endl; // OR
    // cout << " ~a " << (~a) << endl; // NOT
    // cout << " a^b " << (a ^ b) << endl; // XOR

    // int c = 3, d = 2;

    // Left Shift Operator <<
    // Shifts the bits of the left operand to the left side according to the numeric value of the right operand (i.e adding 0 at end);
    // cout << " c << d " << (c << d); // 12

    // Right Shift Operator >>
    // Shifts the bits of the left operand to the right side according to the numeric value of the right operand (i.e removing digits from the end)
    // cout << " c >> d " << (c >> d); // 0

    // Fibonacci Series
    // int n;
    // cin >> n;

    // int a = 0;
    // int b = 1;

    // cout << a << endl << b << endl;

    // for (int i = 0; i < n - 2; i++) {
    //     int next = a + b;
    //     cout << next << endl;
    //     a = b;
    //     b = next;
    // }

    // Prime Number
    // int n;
    // cin >> n;

    // bool isPrime = true;

    // for (int i = 2; i <= n - 1; i++) {
    //     if (n == 2) break;
    //     else if (n % 2 == 0) {
    //         isPrime = false;
    //         break;
    //     }
    // }

    // if (isPrime) cout << "Prime" << endl;
    // else cout << "Not Prime" << endl;

    // Homework Analysis
    // for (int i = 0; i < 5; i++) {
    //     for (int j = i; j <= 5; j++) {
    //         if (i + j == 10) {
    //             break;
    //         }
    //         cout << i << " " << j << endl;
    //     }
    // }

}
